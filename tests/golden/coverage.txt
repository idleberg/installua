installua coverage -- makensis v3.12, 276 commands

  exposed          106
  attribute         68
  lowering-target   28
  language          14
  directive         36
  rejected          24
  todo               0

installua MUI coverage -- Modern UI 2, 255 names

  exposed          130
  internal         108
  rejected          17
  todo               0

installua header coverage -- NSISDIR/Include, 31 headers

  exposed            5
  lowering           6
  replaced           4
  rejected           7
  todo               9

header todo:
  VB6RunTime              rides on `Library.nsh`, so it is classified once that lands
  Win/COM                 Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
  Win/Propkey             Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
  Win/RestartManager      Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
  Win/WinDef              Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
  Win/WinError            Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
  Win/WinNT               Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
  Win/WinUser             Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
  WinCore                 Windows constants beyond messages; not flat like `WinMessages`, since `WinCore` defines `HKLM` as a number
