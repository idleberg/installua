installua coverage -- makensis v3.12, 276 commands

  exposed          104
  attribute         68
  lowering-target   26
  language          14
  directive         37
  rejected          24
  todo               3

todo:
  ReserveFile             `file…` and `/plugin file.dll` are two alternatives in one line, and the second is a plugin DLL (§11)
  LicenseLangString       a license *file* per language, where §15.26's tables hold strings; the page takes one path and there is no per-locale shape for it yet
  InitPluginsDir          the plugin directory and the DLL registration pair, neither of which `plugin` covers yet (§11)

installua MUI coverage -- Modern UI 2, 255 names

  exposed          130
  internal         108
  rejected          17
  todo               0
