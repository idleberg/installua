---
// Both `ThemeProvider` and `ThemeSelect` are overridden to this file: with one
// theme there is no toggle to draw and no stored preference to honour, so this
// renders nothing on purpose. See `astro.config.mjs`.
---
