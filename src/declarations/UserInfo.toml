# UserInfo, ships with NSIS. No shipped doc; counts read from the source,
# `Contrib/UserInfo/UserInfo.c`.
#
# See docs/plugin-reference.md.

[[plugin]]
name = "UserInfo"
method = "getAccountType"
nsis = "UserInfo::GetAccountType"
params = []
outputs = ["string"]
