# Splash, ships with NSIS: <https://nsis.sourceforge.io/Splash_plug-in>.
#
# See docs/plugin-reference.md.

[[plugin]]
name = "Splash"
method = "show"
nsis = "Splash::show"
params = ["uint", "path"]
outputs = ["int"]
